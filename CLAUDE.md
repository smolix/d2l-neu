# D2L-Neu Project Context

Rebuild of "Dive into Deep Learning" (d2l.ai) using Quarto.
See **architecture.md** for full build system documentation.

## Rules

- Source .md files are the source of truth. **Never edit .qmd files** —
  they are generated by d2l_preprocess.py and will be overwritten.
- Use `make` targets for builds (not build.sh or manual venv invocations).
  Makefile handles logging to `logs/` with timestamps.
- Run frameworks sequentially for GPU work — they share GPUs and will
  contend if run in parallel.
- `make clean` keeps `data/`; use `make veryclean` to force re-download.

## Environment

- Quarto 1.7+, Python 3.11+, XeLaTeX + rsvg-convert (PDF only)
- 4× RTX 4090 (24 GB each), NUM_GPUS=4, PARALLEL=8 (2 per GPU)
- UV venvs per framework: `.venv-pytorch`, `.venv-jax`, etc.
- PyTorch and JAX extras are mutually exclusive (conflicting CUDA packages)

## Current status

- **413/413 notebooks pass** (PT 130, TF 90, JAX 65, MX 128)
- **261/261 slides rendered** (PT 75, TF 56, JAX 55, MX 75)
- 192 HTML pages, 4 PDFs, d2l library — zero errors
- Full `make all` takes ~3 hours

### d2l-en source fixes applied (in ../d2l-en, not committed there)
- matplotlib: removed deprecated `use_line_collection` from stem() calls
- gymnasium: `import gym` → `import gymnasium as gym`, removed `env.seed()`
- syne_tune: updated PythonBackend import path
- pandas 3.0: `select_dtypes(include='number')` instead of `dtypes!='object'`
- wikitext-2: updated DATA_HUB URL and hash in bert-dataset.md
