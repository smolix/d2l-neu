# Deep Reinforcement Learning
:label:`chap_deep_rl`

:numref:`chap_reinforcement_learning` ended with a working agent: a policy
gradient method that solves CartPole with a neural network in place of a table.
This chapter is about what breaks when the problem gets larger — the target
moves, the step size is measured in the wrong space, and the data stops being
yours to choose — and about the algorithms that repair each failure:
actor-critic methods, Proximal Policy Optimization, Deep Q-Networks, and
offline reinforcement learning.

```toc
:maxdepth: 2

actor-critic
ppo
dqn
offline-rl
```
