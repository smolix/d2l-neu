# Review & implementation plan — D2L sections 3, 4, 5

**Scope:** *Linear Neural Networks for Regression* (ch. 3), *Linear Neural
Networks for Classification* (ch. 4), *Multilayer Perceptrons* (ch. 5) — 21
content sections + 3 cover pages.
**Goal:** bring these foundational chapters to a standard assignable at the very
best programs (Stanford / MIT / CMU / Berkeley), *exceeding* the in-repo gold
standard (Preliminaries + the Math-for-DL appendix) where possible.
**Primary use of this report:** it is the **input to an implementation phase in
which agents edit the pages**. Every per-file report's **Section 4 is an
executable change spec** (stable IDs, verbatim anchors, drafted content,
files/build touched, acceptance criteria, dependencies). This README is the
**dispatch manifest** that sequences that work.

> **Guardrails baked into every review** (per the author's instructions):
> 1. **Exceed the bar** of Preliminaries / the math appendix.
> 2. **Currency** — these notebooks were written ~2021; flag what is dated or
>    missing in 2026 — *but* (3) **stay in scope**: these are foundational
>    sections inside a larger book; **forward-point** to the chapters that own
>    advanced topics (Optimization, Information Theory, Batch-Norm, Builders'
>    Guide, Attention) rather than importing their content. The scope map is in
>    `_review-guide.md`.

## How to use this document

- **Implementation orchestrator:** read **§Dispatch manifest** below. Settle the
  one book-wide decision (MXNet, §WP-A), run the two unblockers (bib §WP-B,
  figures §WP-C), then fan out one agent per file over §WP-D, handing each agent
  *only* its per-file report. Re-execute the affected notebooks (§WP-E), then
  verify (§WP-F).
- **Author / editor:** read **§Headline verdict**, **§Cross-cutting themes**, and
  **§Open decisions**. Skim the per-file reports for the sections you care about.

---

## Implementation progress (live ledger)

- ✅ **WP-A — MXNet:** decided **KEEP** (see WP-A). All tombstone IDs cancelled.
- ✅ **WP-B — Bibliography:** 7 keys added to `d2l.bib` — `Koh.Sagawa.Marklund.ea.2021`
  (WILDS), `Lipton.Wang.Smola.2018` (BBSE), `Saerens.Latinne.Decaestecker.2002`,
  `Power.Burda.Edwards.ea.2022` (grokking), `Gal.Ghahramani.2016`,
  `Recht.Roelofs.Schmidt.ea.2019`, `Loshchilov.Hutter.2019` (AdamW). *Caught: the
  pre-existing `Loshchilov.Hutter.2016` is the SGDR paper, not AdamW.*
- 🟡 **WP-C — Figures:** pipeline established —
  `tools/gen_mdl_linreg_figures.py` (slug `mdl-linreg-`), importing the shared
  style. **Done:** `mdl-linreg-ridge-geometry` (ridge/lasso constraint geometry;
  exact tangency + L1-corner sparsity; idempotent + **regenerated byte-stably
  under `.venv-pytorch` / matplotlib 3.10.8**, visually reviewed).
  **Remaining (~10):** XOR, double-descent, OO class diagram, VC shattering,
  loss/accuracy branch, MLP architecture, backprop-graph redraw, K-fold,
  pre-generated activation plots, capacity-vs-error restyle.
- 🟡 **WP-D — Text edits:**
  - **§3.7 weight-decay — COMPLETE & reviewed** (WD-2 figure + constraint geometry;
    WD-3 AdamW currency → `sec_adam`; WD-4 MLE/MAP → MLE section; WD-1 false
    "looks similar" claim → accurate convention caveat; WD-7 RKHS trim; WD-5
    binomial + Exercise-1 fixes).
  - **§3.1 linear-regression — text COMPLETE & reviewed** (LR-1a vectorization
    claim fixed to per-op-dispatch lesson; LR-2 intuition-first house-price
    opening; LR-3 geometry-of-least-squares paragraph → `sec_mdl-geometry-…`;
    LR-6a `inference` aside trimmed; LR-7 Exercise-4 ridge sub-part → `sec_weight_decay`).
    *Resolved:* LR-4 — **keep `img/neuron.svg` as-is** (an anatomical reference
    image, correctly outside the geometric house-style convention; not redrawn).
    *Deferred:* LR-1b slide (slides
    out of scope this pass); LR-8 cosmetic cell merge.
  - **§3.4 linear-regression-scratch — COMPLETE & reviewed** (LRS-1 PyTorch seed →
    reproducible recovery, re-executed + captured; LRS-2 anatomy-of-an-SGD-step;
    LRS-3 loss-curve interpretation; LRS-4 forward-pointing Discussion +
    dangling-"later" fix; LRS-6 abs-loss bug fix). *Pending:* tf/mxnet seed (their
    venvs); LRS-7 essay trim (P2).
  - **§3.6 generalization — COMPLETE & reviewed** (GEN-1 **lost polynomial demo
    RESTORED**, executed + captured: clean under/just-right/over result — train→0
    vs test≈4.9e13 at degree 19 — with a strong log-scale U-curve figure,
    seeded/reproducible; GEN-2 dangling-bound fix; GEN-3 bias–variance named →
    §25; GEN-4 double-descent foreshadow → §5.5 + §4.6 pointer; GEN-7 benchmark
    citation; new demo exercise). *Pending:* GEN-5/GEN-6 (P2 polish).
  - **§3.2 oo-design — PyTorch pass done** (OO-1 class-relationship figure
    `mdl-linreg-oo-classes` authored, rendered, reviewed; OO-2 intro rewritten +
    Lightning URL fixed; OO-6 exercises upgraded). *Pending:* OO-8 Summary, OO-7
    NNX note (P2); OO-3 JAX-Trainer trim + OO-5 TF dead-methods (frameworks phase).
  - **16 content files remaining** (ch.3: synthetic-regression-data,
    linear-regression-concise; then all of ch.4 and ch.5).

  **Workflow (per Alex):** PyTorch first across all sections, then render the
  other 3 frameworks in one batch at the end. **Style:** avoid `---` in new prose
  (use commas / parentheses / colons); retrofit the few `---` introduced in the
  done §3.1/3.4/3.6/3.7 during the final cleanup pass.
- 🟡 **WP-E — Re-execution:** **`.venv-pytorch` BUILT ✓**; **pipeline validated
  end-to-end** (edit `.md` → `gmake …executed` → `gmake capture-outputs` →
  committed store; ~38 s/notebook on this Mac). **LRS-1 (PyTorch) DONE** — seeded
  the from-scratch run; recovery now reproduces **bit-for-bit**
  (`w err [3.6e-5, -3.0e-4]`, `b [3e-4]`), re-captured. *Pending:* tf/mxnet
  seeding (needs their venvs — jax is already deterministic) + the LRS-1
  interpreting sentence (add once all four are seeded); then GEN-1 (restore the
  polynomial demo), ICD, KAG-2, DROP-3, SMS/SMC. (WD-1 handled via the caveat.)
- ⏳ **WP-F — Verify:** `make html` + `figure-style-audit` per chapter once complete.

> Figures are content-validated under the available matplotlib; the **byte-stable
> committed SVGs regenerate under `.venv-pytorch` via `make figures`** at build
> time (the generator `.py` is the durable source of truth).

---

## Master status table

Grades are on the "assignable at a top program as-is" scale. All 21 sections are
**correct and already usable** (no F/D); the work is lifting B/B+ → A.

| § | File | Role | Grade | P0 | P1 | P2 | Report |
|---|------|------|:----:|:--:|:--:|:--:|--------|
| 3.1 | linear-regression | Concept anchor: model, loss, MLE, SGD, "as a neuron" | B+ | 1 | 4 | 3 | [LR](03-linear-regression/linear-regression.md) |
| 3.2 | oo-design | Book-wide `Module`/`DataModule`/`Trainer` scaffold | B+ | 1 | 4 | 4 | [OO](03-linear-regression/oo-design.md) |
| 3.3 | synthetic-regression-data | Data pipeline on known ground truth | B− | 1 | 4 | 3 | [SD](03-linear-regression/synthetic-regression-data.md) |
| 3.4 | linear-regression-scratch | From-scratch SGD training loop | B | 1 | 4 | 2 | [LRS](03-linear-regression/linear-regression-scratch.md) |
| 3.5 | linear-regression-concise | Same model via framework APIs | B− | 2 | 3 | 3 | [LRC](03-linear-regression/linear-regression-concise.md) |
| 3.6 | generalization | Over/underfitting, train/val/test, CV | B | 1 | 3 | 3 | [GEN](03-linear-regression/generalization.md) |
| 3.7 | weight-decay | First regularizer; L2 / ridge | B+ | 1 | 4 | 4 | [WD](03-linear-regression/weight-decay.md) |
| 4.1 | softmax-regression | Classification, softmax, cross-entropy | B+ | 0 | 4 | – | [SM](04-linear-classification/softmax-regression.md) |
| 4.2 | image-classification-dataset | Fashion-MNIST loading/viz | B | 0 | 3 | 3 | [ICD](04-linear-classification/image-classification-dataset.md) |
| 4.3 | classification | Generic `Classifier` base (accuracy) | B− | 0 | 4 | 3 | [CLS](04-linear-classification/classification.md) |
| 4.4 | softmax-regression-scratch | From-scratch softmax classifier | B− | 1 | 3 | 3 | [SMS](04-linear-classification/softmax-regression-scratch.md) |
| 4.5 | softmax-regression-concise | Owns numerical stability (log-sum-exp) | B | 1 | 2 | 3 | [SMC](04-linear-classification/softmax-regression-concise.md) |
| 4.6 | generalization-classification | Learning theory: VC, PAC, fund. thm. | B+ | 0 | 4 | 4 | [GC](04-linear-classification/generalization-classification.md) |
| 4.7 | environment-and-distribution-shift | Covariate/label/concept shift, ERM, IW | B+ | 0 | 4 | 5 | [DS](04-linear-classification/environment-and-distribution-shift.md) |
| 5.1 | mlp | Hidden layers, activations, UAT | B+ | 0 | 4 | – | [MLP](05-multilayer-perceptrons/mlp.md) |
| 5.2 | mlp-implementation | MLP scratch + concise | B+ | 0 | 3 | 5 | [MLPI](05-multilayer-perceptrons/mlp-implementation.md) |
| 5.3 | backprop | Forward/backward, computational graphs | B | 0 | 3 | 3 | [BP](05-multilayer-perceptrons/backprop.md) |
| 5.4 | numerical-stability-and-init | Vanishing/exploding, Xavier (+He) | B− | 2 | 4 | 2 | [NSI](05-multilayer-perceptrons/numerical-stability-and-init.md) |
| 5.5 | generalization-deep | Modern generalization (double descent) | B+ | 1 | 5 | 3 | [GD](05-multilayer-perceptrons/generalization-deep.md) |
| 5.6 | dropout | Dropout regularization | B | 1 | 3 | 2 | [DROP](05-multilayer-perceptrons/dropout.md) |
| 5.7 | kaggle-house-price | Capstone: tabular regression on Kaggle | B | 1 | 3 | 4 | [KAG](05-multilayer-perceptrons/kaggle-house-price.md) |

*(P-counts for §4.1 and §5.1 predate the final implementation-spec format — those
two reports carry equivalent drafted content but use the earlier table layout.)*

---

## Headline verdict

These three chapters are **good** — a notch below the Preliminaries gold
standard, uniformly correct, and assignable today. They are **not yet
best-in-class**, and the gap is consistent and fixable: it is rarely the *math*
or the *code logic* (both are sound) and almost always **teaching delivery** —
missing figures, stub Summaries, intuition that lives in the slides but never
reached the prose, and a handful of reproducibility / cross-framework / currency
slips. There is essentially **no scope creep to undo** and **no section to
rewrite wholesale**; this is a surgical lift, not a teardown.

**The five highest-leverage moves, book-wide:**
1. **Decide the MXNet question** (§WP-A) — it unblocks ~18 files at once.
2. **Generate the ~11 missing figures** (§WP-C) — the single biggest teaching
   gap vs. the exemplar chapters, which are figure-rich.
3. **Promote slide-only content into the prose** (§T2) — much of the missing
   intuition already exists, written, in the decks.
4. **Fix the handful of real defects** (§T6): the un-seeded scratch run (LRS-1),
   the PyTorch weight-decay number that contradicts its prose (WD-1), the lost
   polynomial demo (GEN-1), the vectorization-speed claim that overstates the
   measured result (LR-1).
5. **Land the in-scope currency touches** (§T5): finish the double-descent story
   where it belongs (§5.5), add He-init for ReLU where it belongs (§5.4), add the
   trees-beat-nets caveat to the tabular capstone (§5.7) — each one paragraph +
   a pointer, not a new chapter.

---

## Cross-cutting themes

### T1 — MXNet is archived (2023); it is still a co-equal tab everywhere · *book-wide decision*
Apache MXNet was retired by the ASF in 2023. Every code section (15 of 21)
presents MXNet as a first-class, sometimes *leading*, framework tab. This is the
single most pervasive currency issue and the **one decision that gates the most
work**. Exemplar IDs: LR-5, OO (disclaimer), LRS-5, MLPI-3, NSI-5, KAG-4, DROP-4,
ICD-3 (also emits a multi-kB oneDNN stderr wall into committed outputs). See §WP-A.

### T2 — The slides are ahead of the prose · *lift, don't author from scratch*
A recurring authoring artifact: the best intuition was written into the decks and
never back-ported to the body. Reuse it. Confirmed instances: **XOR / linear
inseparability** (MLP-1, in `mlp.md` slides only), **loss-vs-accuracy** (CLS-1),
**the clean log-sum-exp identity** (SMC-1), **trees-vs-nets** (KAG-1), **He-init**
(NSI-1), several **forward-pointers** (NSI-4). Implementation agents should read
the file's `<!-- slides -->` block first and lift verbatim where apt.

### T3 — ~11 missing or dated figures · *one figure work-package* (§WP-C)
The exemplar chapters carry an illustrative figure per idea; these often don't.
Build via the **`mdl-figure`** workflow (committed generator → `img/<id>.svg`, no
drawing code in notebooks), then **`figure-style-audit`**:

| Fig | Section | Depicts |
|-----|---------|---------|
| ridge geometry | 3.7 (WD-2) | loss contours kissing the ‖w‖²≤t ball (+ optional L1 diamond) |
| class diagram | 3.2 (OO-1) | `Module`/`DataModule`/`Trainer` relationships |
| ~~neuron~~ | 3.1 (LR-4) | **resolved: keep `fig_Neuron` as-is** — anatomical reference image, not a geometric schematic; do not redraw |
| loss/accuracy branch | 4.3 (CLS-4) | logits → {cross-entropy/loss, argmax/accuracy} |
| shattering | 4.6 (GC-2) | 3 points shattered by a line; 4 / XOR cannot |
| XOR | 5.1 (MLP-1) | linear inseparability + the 2-neuron fix |
| ~~activation plots~~ | 5.1 (MLP-2) | **reconsidering** — likely KEEP inline as computed `d2l.plot` teaching cells (a function plot teaches like a loss curve); pre-generate only if truly decorative |
| MLP architecture | 5.2 (MLPI-6) | **check redundancy with §5.1 `mlp.svg` first** — may not be needed |
| backprop graph | 5.3 (BP-2) | redraw `forward.svg` (a schematic) w/ a backward pass — **only if the current one is inadequate**; confirm first |
| double descent | 5.5 (DD-1) | classical U → interpolation threshold → 2nd descent |
| K-fold | 5.7 (KAG-8) | k-fold CV split schematic |
| capacity-vs-error | 3.6 (GEN) | restyle the legacy SVG **only if needed** for consistency; confirm first |

### T4 — Stub Summaries → real "Summary and Discussion" with forward-pointers
Many sections end on a 3–5-line stub that doesn't close the arc. Pattern fix:
rewrite as a Discussion that names what was learned and forward-points. IDs:
SD-4, LRS-4, MLPI-1, SMS-1, DROP-1, KAG-3, CLS-2, SMC (training-print), plus
OO-8.

### T5 — In-scope currency touches (forward-pointer flavored)
Modernize *within each section's lane*: **double descent + implicit reg +
grokking** in §5.5 (DD-1/DD-2 — its rightful home), **He/Kaiming init** in §5.4
(NSI-1 — the ReLU counterpart to Xavier belongs here), **trees-beat-nets on
tabular** in §5.7 (KAG-1), **AdamW / L2≠decay** as one line → Optimization (WD-3),
**calibration** caveat in §4.1/§4.3 (Guo 2017, already cited), **modern shift /
WILDS** pointer in §4.7 (DS-2), GELU/SwiGLU as a **pointer** only in §5.1 (MLP-4).
None of these expand into a later chapter's territory.

### T6 — Real defects (correctness / reproducibility / cross-framework) · *highest urgency*
- **GEN-1 [P0]** — `generalization.md` has **no code at all**; the runnable
  polynomial under/just/over-fit demo (which upstream d2l *runs*) was lost. The
  chapter asserts the U-curve but never shows it.
- **LRS-1 [P0]** — the from-scratch run is **un-seeded**, so the headline "we
  recover the true w, b" is not reproducible.
- **WD-1 [P0]** — PyTorch concise ‖w‖²=0.013 (≈8× the other three frameworks &
  the scratch run); the prose "looks similar to from scratch" is **false for the
  primary framework** (root cause: `nn.MSELoss` drops the ½). Fix or teach it.
- **LR-1 [P0]** — the "orders of magnitude" vectorization claim overstates the
  committed PyTorch result (~58×); reframe around dispatch overhead.
- **Cross-framework output discrepancies** users will hit by switching tabs:
  ICD-1 (shape 1×32×32 vs 28×28), SD-1 (31 vs 32 batches), WD-1 (norms).
- **DROP-3** — the JAX scratch dropout uses a deterministic mask every step.
- **LRC-2** — PyTorch summary claims you must specify input dims, but the code
  uses `LazyLinear` (which doesn't).

### T7 — Missing `d2l.bib` keys · *one bib work-package* (§WP-B)
Several authored changes cite keys not in `d2l.bib`. Add (verify first — some
turned out to be present, e.g. Belkin double descent & He init **are** in bib but
**uncited**, fixed by DD-3 / NSI-1):
`Koh.*2021` (WILDS, DS-2) · `Lipton.Wang.Smola.2018` (BBSE, DS-1) ·
`Saerens.*2002` (label shift, DS-1) · `Power.*2022` (grokking, DD-2a) ·
`Gal.Ghahramani.2016` (MC dropout, DROP-5) · `recht2019imagenet` (GEN-7) ·
Recht-et-al. empirical-bound counterpoint (GC-8) · verify `loshchilov2019decoupled` (WD-3).

### T8 — Scratch ↔ concise coordination (don't duplicate, do hand off)
The from-scratch/concise pairings must coordinate, not repeat. Key: §4.4 raises
the naive-softmax instability and must **forward-point** to §4.5, which **owns**
the log-sum-exp fix and must actually finish that derivation (SMS-2 ↔ SMC-1).
Same shape for weight-decay conventions (§3.7) and init deferral (§5.2 → §5.4).

---

## Dispatch manifest

Ordered so unblockers run first and independent edits parallelize. IDs reference
each per-file report's Section 4.

### WP-A — Book-wide decision: MXNet — ✅ RESOLVED (2026-06-13): **KEEP**
Decision (author): **keep MXNet as a co-equal, tab-selectable framework.** A
workable Apple-silicon wheel is available (`github.com/smolix/mxnet`), and the
tab mechanism means an extra framework costs the reader nothing. Consequences for
this plan: **all `*-MXNet` tombstone / de-emphasis / "archived" change IDs are
CANCELLED** (LR-5, OO disclaimer, LRS-5, MLPI-3, NSI-5(tab), KAG-4, DROP-4,
MLP-3, SMS(tab), SMC(tab), etc.). **Retain** the MXNet *code-quality* fixes that
are independent of keep/drop — e.g. suppressing the multi-kB oneDNN/storage
stderr wall in committed outputs (ICD-3, SMS-5) and adding missing inline
comments (CLS-7). No file is blocked anymore; the plan is fully unblocked.

### WP-B — Bibliography *(mechanical; unblocks several authored changes)*
Add/verify the keys in §T7 in `d2l.bib`. Stub entries are drafted in the DS, GD,
GC, DROP, GEN reports. **Depends on:** none. **Done when:** `make html` resolves
all `:cite:`/`:citet:` with no missing-key warnings.

### WP-C — Figures *(parallel; `mdl-figure` workflow → commit SVGs)*
Generate the 11 figures in §T3, then run **`figure-style-audit`**. **Depends on:**
none (figure content is specified in each report). **Done when:** each `img/*.svg`
is byte-idempotent, captioned, `:label:`-ed, referenced by `:numref:`, and renders
in HTML + PDF. Several text edits (WD-2, MLP-1, GC-2, DD-1, CLS-4…) **depend on**
their figure existing.

### WP-D — Per-file text edits *(21 packages; mostly parallel)*
One agent per file, given **only** that file's report. Pure-prose files
(`linear-regression`? no — has code; the pure-prose set is **GEN-text**, **GC**,
**DS**, **BP**, **GD**, **MLP-prose**) can proceed immediately. Files whose edits
reference a new figure or bib key **depend on** WP-B/WP-C. Files needing
re-execution route their code edits to WP-E. Suggested grouping:
- **Independent now:** GC, DS (after WP-B), BP (text), MLP (text), SMC-1, SMS-2/3,
  CLS-1/2/3, OO-2/3/4/5, SD-2/3/4, LR-2/3, LRS-2/3/4, NSI-2/3, MLPI-1/2, DROP-1/2.
- **After their figure (WP-C):** WD-2, CLS-4, GC-2, MLP-1, DD-1, OO-1, MLPI-6,
  BP-2, KAG-8, LR-4.
- **After WP-A (MXNet):** every `*-MXNet`/tombstone ID.

### WP-E — Re-execution + re-capture *(CPU-only; needs the build pipeline)*
These edits change code/outputs and must be re-run, then blessed into `outputs/`
(`make -B _notebooks/<fw>/<ch>/<f>.executed && make capture-outputs FILES=<ch>/<f>.md`).
All are **CPU notebooks** (Fashion-MNIST / synthetic / Kaggle) — runnable on the
Apple-silicon laptop, no GPU. Items: **GEN-1** (add+run demo), **LRS-1** (seed →
re-run ×4), **WD-1** (fix → re-run), **ICD-1/ICD-3** (shape cells + MXNet stderr),
**KAG-2** (MLP upgrade → re-run), **DROP-3** (JAX key → re-run JAX), **SMS-4 /
SMC** (add accuracy print → re-run), **SD-7** (rename `noise`→`eps` needs
`make lib` first). **Depends on:** the corresponding WP-D edit. **Done when:**
the manifest text outputs match the prose claims and the freshness gate is clean.

### WP-F — Verify
`make html` clean (no warnings); `figure-style-audit` per touched chapter; a link
check on new URLs; spot-render the touched pages on the served site. Optionally
re-run a thin reviewer pass on the highest-churn files (GEN, NSI, GD, KAG).

**Critical path:** WP-A (decision) ∥ WP-B ∥ WP-C → WP-D → WP-E → WP-F.

---

## Cover-page resources sections

The author asked for a "Resources and Further Reading" block on every chapter
cover page, matching the Preliminaries / math-appendix house style. Status:

- **Ch. 3 (regression)** `index.md` — **already has** a strong block. Minor
  net-new suggestions only.
- **Ch. 4 (classification)** `index.md` — **already has** a good block, **but it
  links the retired legacy d2l.ai information-theory URL** (flagged by SM); repoint
  to `:numref:`sec_mdl-information_theory``. Optional specialist additions
  (Shalev-Shwartz & Ben-David; MacKay; WILDS).
- **Ch. 5 (MLP)** `index.md` — **has none; must be created.**

Ready-to-paste content for all three (MLP newly authored, scope-filtered) is in
**[`resources-sections.md`](resources-sections.md)**.

---

## Open decisions for the author

1. **MXNet policy (WP-A).** ✅ **RESOLVED: keep** (co-equal, tab-selectable; an
   Apple-silicon wheel exists at `github.com/smolix/mxnet`). All MXNet
   tombstone/de-emphasis IDs are cancelled; only MXNet code-quality fixes remain.
2. **JAX: Flax `linen` → `nnx`.** Multiple files use legacy `linen`; `d2l/jax.py`
   imports it. A migration is **book-wide**, not per-file — out of scope here; flag
   as a tracked follow-up.
3. **TensorFlow: Keras 2 vs Keras 3.** A couple of files use pre-Keras-3 idioms
   (`tf.keras.losses.Reduction`). Decide chapter-wide targeting.
4. **How far to push currency.** The plan keeps every modern result in-scope
   (pointer or one paragraph in its rightful home). Confirm that's the intended
   ceiling, or raise it for specific sections (e.g. a fuller double-descent
   treatment in §5.5).

---

## Method & provenance

- **Inputs per file:** the source `.md` (source of truth); the rendered page on
  `d2l.smola.org`; the **executed-output manifests** in
  `outputs/<fw>/<chapter>/<file>.json` (text outputs cross-checked against prose
  claims) + the rendered plot SVGs.
- **Bar:** `chapter_preliminaries/calculus.md` and the `chapter_mdl-*` appendix.
- **Shared rubric:** [`_review-guide.md`](_review-guide.md) (mission, scope map,
  currency lens, per-framework code checklist, the Section-4 implementation-spec
  format).
- **Topic research:** [`_research/mlp.md`](_research/mlp.md) (regression/
  classification digests were deferred — scope discipline made the per-file
  expertise + scope map sufficient; classification web-research findings are
  folded into the GC/DS reports and the ch. 4 resources list).
- **Models:** opus for conceptually/mathematically deep sections, sonnet for the
  more mechanical code/data sections.
- **Not reviewed:** slides (explicitly out of scope this pass — but see §T2; they
  are a content *source* for the prose lift).
